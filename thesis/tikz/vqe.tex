\documentclass[tikz]{standalone}

\usetikzlibrary{arrows,shadows,positioning,fit}

\begin{document}

\tikzset{
    frame/.style={
            draw,
            text width=6em,
            text centered,
            minimum height=4em,
            drop shadow,
            fill=orange!40,
            rounded corners,
        },
}
\begin{tikzpicture}[font=\sffamily, thick, node distance=4cm, framed, background rectangle/.style={draw=white}]
    \node[frame] (qua)                {Quantum Node};
    \node[frame] (cla) [right of=qua] {Classical Node};

    \draw[-stealth] (qua) to [bend left=40] (cla) node [above left = .5cm and -1.3cm of cla] {Compute $\expval{U(\boldsymbol{\theta})}{\psi}$};
    \draw[-stealth] (cla) to [bend left=40] (qua) node [below left = .5cm and -1.5cm of cla] {Modify parameters $\boldsymbol{\theta}\to \boldsymbol{\theta}'$};
\end{tikzpicture}

\end{document}